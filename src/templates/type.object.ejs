    /**
    * <%= prop.title%>
    * <%= prop.description %>
    */
    <% sys.each(decs, function(v){ %><%- v %>
    <%}); _%>
@IsInstance(<%= prop.$ref %>)
    <%= prop.name %><%= prop.required ? "" : "?" %>: <%= prop.$ref %><% if ( !sys.isUndefined( prop.defaultValue ) ){ %> = <%= prop.defaultValue %><% } %>;
